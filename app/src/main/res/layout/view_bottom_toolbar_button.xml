<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:background="@color/colorPrimary"
    tools:layout_height="@dimen/bottom_toolbar_height"
    tools:layout_width="200dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginEnd="@dimen/activity_horizontal_margin_small"
        android:layout_marginStart="@dimen/activity_horizontal_margin_small"
        android:gravity="center">

        <ImageView
            android:id="@+id/bottom_toolbar_icon"
            android:layout_width="@dimen/bottom_toolbar_image_height"
            android:layout_height="@dimen/bottom_toolbar_image_height"
            android:contentDescription="@string/toolbar_icon"
            tools:src="@drawable/ic_dva" />

        <TextView
            android:id="@+id/bottom_toolbar_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/activity_horizontal_margin_small"
            android:textAppearance="@style/TextAppearance.AppCompat.Small"
            android:textColor="@color/light_text_color"
            tools:text="Home" />

    </LinearLayout>

</RelativeLayout>